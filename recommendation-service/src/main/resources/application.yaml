spring:
  application:
    name: recommendation-service
  cloud:
    client:
      hostname: fitness-recommendation-project
  mongodb:
    uri: mongodb://rahul2711:Ranaghat1@mongo:27017/fitnessRecommendations-db?authSource=admin

server:
  port: 4004

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8080/eureka
  instance:
    prefer-ip-address: false

gemini:
  api:
    key: ${GEMINI_API_KEY}
  model: gemini-3-flash-preview

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking